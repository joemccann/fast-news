# WebSocket Relay Server Configuration
# =====================================

# REQUIRED: Upstream WebSocket URL to connect to
# The server will connect to this URL and relay filtered messages
UPSTREAM_WS_URL=wss://your-upstream-websocket-server.com/ws

# OPTIONAL: Server port (default: 3001)
PORT=3001

# OPTIONAL: Heartbeat interval in milliseconds (default: 30000)
HEARTBEAT_INTERVAL_MS=30000

# OPTIONAL: Upstream reconnection delay in milliseconds (default: 5000)
RECONNECT_DELAY_MS=5000
